import 'package:flutter/material.dart';

class ReportTabView extends StatelessWidget {
  const ReportTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('Feddback View')],
    );
  }
}

Widget listContainer(String name, String mob, String place, String count) {
  return Container();
}
